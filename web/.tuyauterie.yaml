version: 1

build:
  type: node
  upload_artifacts: false
  path: web
  artifact_name: pianoter-web
  upload_artifact: false
  test_cmd: "npm test"

release:
  rolling: false
  on-tag: true

deploy:
  gitops:
    enable: true
    repository: "notarock/gitops"
    manifest: "pianoter/frontend-deployment.yaml"
