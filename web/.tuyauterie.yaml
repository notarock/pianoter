version: 1

build:
  type: node
  upload_artifacts: false
  path: web
  artifact_name: pianoter-web
  upload_artifact: false
  test_cmd: "npm test && npm run cy:run" 

release:
  rolling: false
  on-tag: true
