version: 1

build:
  type: golang
  dockerfile: Dockerfile
  artifact_name: pianoter-backend
  upload_artifact: false

release:
  rolling: false
  on-tag: true

deploy:
  gitops:
    enable: true
    repository: "notarock/gitops"
    manifest: "pianoter/backend-deployment.yaml"
